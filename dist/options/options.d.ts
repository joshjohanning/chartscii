import { CustomizationOptions } from '../types/types';
export declare const defaultOptions: CustomizationOptions;
declare class Options {
    constructor(options: CustomizationOptions);
}
export default Options;
